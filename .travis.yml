matrix:
  include:
    - language: java
      jdk: openjdk8
      before_install:
        - cd java
      install:
        - ./mvnw test-compile -DskipTests
      script:
        - ./mvnw test jacoco:report
notifications:
  email: false